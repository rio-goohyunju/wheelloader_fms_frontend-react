import defaultTheme from "../common-styles/src/defaultTheme";
import { ITheme } from "../common-styles/src/types";
import colors from "./colors";

export type ThemeType = ITheme;
export { colors, defaultTheme as theme };
